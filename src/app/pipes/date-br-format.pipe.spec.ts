import { DateBrFormatPipe } from './date-br-format.pipe';

describe('DateBrFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateBrFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
