import { MoneyBrFormatPipe } from './money-br-format.pipe';

describe('MoneyBrFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MoneyBrFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
